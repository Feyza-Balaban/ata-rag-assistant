# Tests

The tests cover retrieval relevance, low-confidence refusal, the `/ask` API
contract, health status, source links, and optional bearer authentication.

Run from the repository root:

```powershell
python -m pytest -q
```
